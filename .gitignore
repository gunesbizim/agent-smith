node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
*.log

# Runtime artifacts from test/CLI runs
.claude/
.cursor/
.mcp.json
.sentrux/*
!.sentrux/rules.toml
!.sentrux/baseline.json
docs/architecture/

# Runtime engine run logs (transient; events.jsonl is rebuildable, never committed)
.agent-smith/runs/

# Dashboard lifecycle refcount (transient; written by the dashboard auto-start/stop hooks)
.agent-smith/dashboard.json

# Temp files
a
coverage/

# Obsidian vault — now committed (public project documentation); see vault/agent-smith/index.md

# Playwright MCP screenshots/traces — generated artifacts, never commit
.playwright-mcp/
docs/screenshots/
.gitnexus

# Claude Code session transcripts accidentally dropped at repo root
/????-??-??-*.txt
local_cache/
